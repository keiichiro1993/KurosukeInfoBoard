<UserControl
    x:Class="KurosukeInfoBoard.Controls.CalendarItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KurosukeInfoBoard.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="400">
    <UserControl.Resources>
        <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
    </UserControl.Resources>

    <Grid x:Name="rootGrid" SizeChanged="rootGrid_SizeChanged">
        <Grid Margin="0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="AUTO"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Margin="10,0" x:Name="DateTextBlock" FontSize="20"/>
            <ListView Grid.Row="1" x:Name="EventsListView" SelectionMode="None" IsItemClickEnabled="True" DisplayMemberPath="summary">
            </ListView>
        </Grid>
        <Border BorderThickness="1" BorderBrush="Gray" VerticalAlignment="Top" Margin="10,0"/>
    </Grid>
</UserControl>
