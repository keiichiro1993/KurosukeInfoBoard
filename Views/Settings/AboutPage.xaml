<Page
    x:Class="KurosukeInfoBoard.Views.Settings.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KurosukeInfoBoard.Views.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="AUTO"/>
                <RowDefinition Height="AUTO"/>
                <RowDefinition Height="AUTO"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <TextBlock Margin="10" FontSize="30">About Kurosuke Home Dashboard</TextBlock>
                <Border BorderBrush="Gray" BorderThickness="0,0,0,2"/>
            </Grid>
            <Grid Grid.Row="1">
                
            </Grid>

            <Grid Grid.Row="2">
                <TextBlock Margin="10,20,10,10" FontSize="30">License</TextBlock>
                <Border BorderBrush="Gray" BorderThickness="0,0,0,2"/>
            </Grid>
            <controls:MarkdownTextBlock Grid.Row="3" Text="**This is *Markdown*!**">
            </controls:MarkdownTextBlock>
        </Grid>
    </Grid>
</Page>
